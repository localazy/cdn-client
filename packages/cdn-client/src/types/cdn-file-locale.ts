// #region typedef
export type CdnFileLocale = {
  /**
   * Locale code.
   */
  locale: string;

  /**
   * Boolean indicating if this is the base locale.
   */
  isBaseLocale: boolean;

  /**
   * File URI.
   */
  uri: string;
};
// #endregion typedef
